import React, { ReactElement } from "react";
import { ReactSVG } from "react-svg";

import svg from "./svg.svg";

const Thermoicon = (): ReactElement => <ReactSVG src={svg} />;

export default Thermoicon;
